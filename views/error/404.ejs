<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>404 - Page Not Found</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <!-- Optional: if you use a base layout, you can wrap this in your layout instead -->

  <style>
    :root {
      --bg-color: #050608;
      --accent: #4f46e5;
      --accent-soft: rgba(79, 70, 229, 0.4);
      --text-main: #e5e7eb;
      --text-muted: #9ca3af;
      --danger: #f97373;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      background: radial-gradient(circle at top, #111827 0, #020617 55%, #000 100%);
      color: var(--text-main);
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      overflow: hidden;
    }

    .wrapper {
      position: relative;
      padding: 3rem 2rem;
      max-width: 480px;
      width: 100%;
      text-align: center;
      border-radius: 1.5rem;
      background: radial-gradient(circle at top left, rgba(79, 70, 229, 0.12), transparent 60%),
                  radial-gradient(circle at bottom right, rgba(236, 72, 153, 0.12), transparent 55%),
                  rgba(15, 23, 42, 0.93);
      box-shadow:
        0 0 40px rgba(0, 0, 0, 0.8),
        0 0 120px rgba(56, 189, 248, 0.25);
      backdrop-filter: blur(18px);
      border: 1px solid rgba(148, 163, 184, 0.25);
      overflow: hidden;
    }

    /* Lightning background flashes */
    .lightning {
      pointer-events: none;
      position: fixed;
      inset: 0;
      background:
        radial-gradient(circle at 20% 10%, rgba(250, 250, 255, 0.8), transparent 55%),
        radial-gradient(circle at 80% 30%, rgba(190, 227, 248, 0.7), transparent 50%),
        radial-gradient(circle at 40% 80%, rgba(129, 140, 248, 0.6), transparent 55%);
      mix-blend-mode: screen;
      opacity: 0;
      animation: lightningFlash 6s infinite;
      z-index: -1;
    }

    @keyframes lightningFlash {
      0%, 60%, 100% { opacity: 0; }
      62% { opacity: 0.9; }
      64% { opacity: 0.15; }
      66% { opacity: 0.7; }
      70% { opacity: 0; }
    }

    /* Animated 404 text */
    .code {
      font-size: clamp(4rem, 10vw, 5.5rem);
      font-weight: 800;
      letter-spacing: 0.25em;
      text-indent: 0.25em;
      margin-bottom: 0.75rem;
      color: var(--text-main);
      text-shadow:
        0 0 8px rgba(248, 250, 252, 0.6),
        0 0 24px rgba(79, 70, 229, 0.8);
      animation: codeGlow 3s ease-in-out infinite;
    }

    @keyframes codeGlow {
      0%, 100% {
        text-shadow:
          0 0 6px rgba(248, 250, 252, 0.6),
          0 0 16px rgba(79, 70, 229, 0.7);
      }
      50% {
        text-shadow:
          0 0 12px rgba(248, 250, 252, 0.9),
          0 0 32px rgba(59, 130, 246, 0.9);
      }
    }

    .title {
      font-size: 1.35rem;
      margin-bottom: 0.75rem;
      color: var(--text-main);
    }

    .message {
      font-size: 0.95rem;
      color: var(--text-muted);
      margin-bottom: 1.75rem;
      line-height: 1.5;
    }

    /* Route info if provided */
    .path {
      font-family: "JetBrains Mono", ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
      font-size: 0.8rem;
      padding: 0.5rem 0.9rem;
      border-radius: 999px;
      background: rgba(15, 23, 42, 0.9);
      border: 1px solid rgba(148, 163, 184, 0.35);
      color: var(--danger);
      margin: 0 auto 1.5rem;
      max-width: 280px;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }

    /* Button / links */
    .actions {
      display: flex;
      justify-content: center;
      gap: 0.75rem;
      flex-wrap: wrap;
      margin-bottom: 0.75rem;
    }

    .btn {
      position: relative;
      padding: 0.65rem 1.4rem;
      border-radius: 999px;
      font-size: 0.9rem;
      font-weight: 500;
      border: 1px solid transparent;
      cursor: pointer;
      text-decoration: none;
      transition: all 0.18s ease-out;
      display: inline-flex;
      align-items: center;
      gap: 0.4rem;
    }

    .btn-primary {
      color: #e5e7eb;
      background: radial-gradient(circle at top left, #4f46e5, #1d4ed8);
      box-shadow:
        0 0 14px rgba(59, 130, 246, 0.7),
        0 0 32px rgba(79, 70, 229, 0.9);
    }

    .btn-primary::after {
      content: "";
      position: absolute;
      inset: 0;
      border-radius: inherit;
      border: 1px solid rgba(248, 250, 252, 0.28);
      opacity: 0;
      transition: opacity 0.18s ease-out;
    }

    .btn-primary:hover {
      transform: translateY(-1px) scale(1.01);
      box-shadow:
        0 0 22px rgba(59, 130, 246, 0.9),
        0 0 52px rgba(59, 130, 246, 0.9);
    }

    .btn-primary:hover::after {
      opacity: 1;
    }

    .btn-ghost {
      color: var(--text-muted);
      background: rgba(15, 23, 42, 0.8);
      border-color: rgba(148, 163, 184, 0.4);
    }

    .btn-ghost:hover {
      background: rgba(30, 64, 175, 0.35);
      color: var(--text-main);
      border-color: rgba(129, 140, 248, 0.8);
    }

    .hint {
      font-size: 0.75rem;
      color: #6b7280;
    }

    @media (max-width: 480px) {
      .wrapper {
        margin: 1.25rem;
        padding: 2.25rem 1.4rem;
      }
      .code {
        letter-spacing: 0.18em;
      }
    }
  </style>
</head>
<body>
  <!-- lightning background layer -->
  <div class="lightning"></div>

  <div class="wrapper">
    <div class="code">404</div>
    <h1 class="title">Lost in the dark</h1>

    <p class="message">
      The page you are looking for has been swallowed by the stormy **void**.
      Try going back home or double‑check the address.
    </p>

    <% if (typeof url !== 'undefined') { %>
      <div class="path">
        /<%= url.replace(/^\//, '') %>
      </div>
    <% } %>

    <div class="actions">
      <a href="/" class="btn btn-primary">
        <span>Back to home</span>
        <span>⮌</span>
      </a>

      <button type="button" class="btn btn-ghost" onclick="window.history.back()">
        Go back
      </button>
    </div>

    <p class="hint">
      Error code: 404 • Page not found
    </p>
  </div>
</body>
</html>
