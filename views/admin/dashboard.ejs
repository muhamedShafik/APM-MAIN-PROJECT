<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title><%= title || 'TomatoBox Admin' %></title>
    <link rel="stylesheet" href="../../public/admin.css" />
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

  </head>
  <body class="tb-body">
    <div class="tb-shell">
      <!-- Sidebar -->
      <aside class="tb-sidebar">
        <div class="tb-logo" style="font-size: 22px">TomatoBox</div>
        <nav class="tb-nav">
          <a href="/admin/dashboard" class="tb-nav-item tb-nav-item--active"
            >Dashboard</a
          >
          <a href="/admin/orders" class="tb-nav-item">Orders</a>
          <a href="/admin/daily-prices" class="tb-nav-item">Daily prices</a>
          <a href="/admin/users" class="tb-nav-item">Users</a>
          <a href="/admin/payments" class="tb-nav-item">Payments</a>
        </nav>
      </aside>

      <!-- Main area -->
      <main class="tb-main">
        <!-- Top bar with profile dropdown -->
        <header class="tb-topbar">
          <div class="tb-topbar-title">
            <h1>Dashboard</h1>
          </div>

          <div class="tb-topbar-user">
            <div class="tb-user-menu">
              <div class="tb-user-avatar">
                <span
                  ><%= (user && user.name ? user.name[0] : 'A').toUpperCase()
                  %></span
                >
              </div>
              <div class="tb-user-text">
                <p class="tb-user-name">
                  <%= user && user.name ? user.name : 'Admin User' %>
                </p>
                <p class="tb-user-role">
                  <%= user && user.role ? user.role : 'Admin' %>
                </p>
              </div>
              <button class="tb-user-caret" type="button">▾</button>

              <div class="tb-user-dropdown">
                <form action="/admin/logout" method="post">
                  <a
                    href="/admin/logout"
                    style="
                      color: #e5e7eb;
                      display: block;
                      padding: 8px 14px;
                      text-decoration: none;
                    "
                  >
                    Logout
                  </a>
                </form>
              </div>
            </div>
          </div>
        </header>

        <!-- Main content inside dark body -->
        <div class="tb-content">
          <!-- Today’s Overview cards -->
          <section class="tb-section">
            <h2>Today’s Overview</h2>
            <div class="tb-cards">
              <div class="tb-card">
                <p class="tb-card-label">Today’s Price per Box</p>
                <p class="tb-card-value">
                  ₹<%= todayPricePerBox || '120.00' %>
                </p>
                <p class="tb-card-meta">
                  as of <%= todayDate || 'May 15, 2024' %>
                </p>
              </div>
              <div class="tb-card">
                <p class="tb-card-label">Total Boxes Ordered Today</p>
                <p class="tb-card-value"><%= boxesToday || 48%></p>
              </div>
              <div class="tb-card">
                <p class="tb-card-label">Amount Collected Today</p>
                <p class="tb-card-value">
                  ₹<%= amountCollectedToday || '0' %>
                </p>
              </div>
              <div class="tb-card">
                <p class="tb-card-label">Pending Amount Today</p>
                <p class="tb-card-value">
                  ₹<%= pendingAmountToday || '5,800.00' %>
                </p>
              </div>
            </div>
          </section>

          <!-- Tomorrow’s Orders table -->
          <section class="tb-section">
            <h2>Tomorrow’s Orders</h2>

            <table class="tb-table">
              <thead>
                <tr>
                  <th>Shop</th>
                  <th>Location</th>
                  <th>Boxes</th>
                  <th>Discount</th>
                  <th>Delivery Date</th>
                  <th>Actions</th>
                </tr>
              </thead>

              <tbody>
                <% tomorrowOrders.forEach(order => { %>
                <tr>
                  <td><%= order.shop.name %></td>
                  <td><%= order.shop.location %></td>
                  <td><%= order.boxes %></td>
                  <td>₹<%= order.discountPerBox %></td>
                  <td><%= order.deliveryDate %></td>
                  <td>
                    <button
                      class="tb-icon-btn"
                      onclick="openEditModal('<%= order._id %>')"
                    >
                      ✏️
                    </button>
                    <button
                      class="tb-icon-btn tb-icon-btn--danger"
                      onclick="openDeleteModal('<%= order._id %>')"
                    >
                      ✖️
                    </button>
                  </td>
                </tr>
                <% }) %>
              </tbody>
            </table>

            <!-- Pagination -->
            <!-- Pagination -->
            <div class="tb-pagination">
              <div class="tb-pagination-info">
                Page <strong><%= page %></strong> of
                <strong><%= totalPages %></strong>
              </div>

              <div class="tb-pagination-actions">
                <% if (page > 1) { %>
                <a
                  href="/admin/dashboard?page=<%= page - 1 %>"
                  class="tb-page-btn"
                >
                  Previous
                </a>
                <% } else { %>
                <span class="tb-page-btn tb-page-btn--disabled">Previous</span>
                <% } %> <% if (page < totalPages) { %>
                <a
                  href="/admin/dashboard?page=<%= page + 1 %>"
                  class="tb-page-btn tb-page-btn--primary"
                >
                  Next
                </a>
                <% } else { %>
                <span class="tb-page-btn tb-page-btn--disabled">Next</span>
                <% } %>
              </div>
            </div>
          </section>

          <!-- Last 7 Days Trends -->
          <section class="tb-section">
            <h2>Last 7 Days Trends</h2>
         <div class="tb-card tb-chart-card">
  <div class="tb-chart-wrapper">
    <canvas id="sevenDayChart"></canvas>
    <script>
  const __CHART_DATA__ = {
    labels: JSON.parse('<%- JSON.stringify(chartLabels || []) %>'),
    collected: JSON.parse('<%- JSON.stringify(chartCollected || []) %>'),
    pending: JSON.parse('<%- JSON.stringify(chartPending || []) %>')
  };
</script>

  </div>
</div>


          </section>

          <footer class="tb-footer">
            © <%= new Date().getFullYear() %> Tomatobox Admin. All rights
            reserved.
          </footer>
        </div>
      </main>
    </div>
    <!-- edit delete modal  -->

    <div id="editOrderModal" class="tb-modal">
      <div class="tb-modal-content">
        <h3>Edit Order</h3>

        <form id="editOrderForm" method="POST">
          <input type="hidden" name="orderId" id="editOrderId" />

          <label class="tb-label">Number of Boxes</label>
          <input
            type="number"
            class="tb-input"
            id="editBoxes"
            name="boxes"
            required
          />

          <label class="tb-label">Discount Per Box</label>
          <input
            type="number"
            class="tb-input"
            id="editDiscount"
            name="discountPerBox"
          />

          <label class="tb-label">Delivery Date</label>
          <input
            type="date"
            class="tb-input"
            id="editDeliveryDate"
            name="deliveryDate"
          />

          <button type="submit" class="tb-btn-primary">Update</button>
        </form>

        <button class="tb-modal-close" onclick="closeEditModal()">Close</button>
      </div>
    </div>

    <div id="deleteOrderModal" class="tb-modal">
      <div class="tb-modal-content">
        <h3>Delete Order?</h3>
        <p>This action cannot be undone.</p>

        <form id="deleteOrderForm" method="POST">
          <input type="hidden" id="deleteOrderId" name="orderId" />
          <button
            class="tb-btn-primary"
            style="background: #dc2626"
            type="submit"
          >
            Delete
          </button>
        </form>

        <button class="tb-modal-close" onclick="closeDeleteModal()">
          Cancel
        </button>
      </div>
    </div>

    <script>
      const menu = document.querySelector(".tb-user-menu");
      const caret = document.querySelector(".tb-user-caret");

      if (menu && caret) {
        caret.addEventListener("click", (e) => {
          e.stopPropagation();
          menu.classList.toggle("tb-user-menu--open");
        });

        document.addEventListener("click", () => {
          menu.classList.remove("tb-user-menu--open");
        });
      }

      // edit delete modal js
      async function openEditModal(id) {
        const res = await fetch(`/admin/orders/${id}`);
        const order = await res.json();

        document.getElementById("editOrderId").value = id;
        document.getElementById("editBoxes").value = order.boxes;
        document.getElementById("editDiscount").value = order.discountPerBox;
        document.getElementById("editDeliveryDate").value = order.deliveryDate;

        document.getElementById(
          "editOrderForm"
        ).action = `/admin/orders/${id}/update`;

        document.getElementById("editOrderModal").style.display = "flex";
      }

      function closeEditModal() {
        document.getElementById("editOrderModal").style.display = "none";
      }

      function openDeleteModal(id) {
        document.getElementById("deleteOrderId").value = id;
        document.getElementById(
          "deleteOrderForm"
        ).action = `/admin/orders/${id}/delete`;
        document.getElementById("deleteOrderModal").style.display = "flex";
      }

      function closeDeleteModal() {
        document.getElementById("deleteOrderModal").style.display = "none";
      }

      // sweet alert

   const { labels, collected, pending } = __CHART_DATA__;

  const ctx = document
    .getElementById("sevenDayChart")
    .getContext("2d");

  new Chart(ctx, {
    type: "line",
    data: {
      labels,
      datasets: [
        {
          label: "Collected",
          data: collected,
          borderWidth: 2,
          tension: 0.4
        },
        {
          label: "Pending",
          data: pending,
          borderWidth: 2,
          tension: 0.4
        }
      ]
    },
    options: {
      responsive: true,
      maintainAspectRatio: false
    }
  });
    </script>
    
  </body>
  <style>
    /* 
sweet alert */

    .dark-swal-popup {
      background: rgba(0, 0, 0, 0.6) !important;
      color: #fff !important;
      border-radius: 8px;
      font-size: 14px;
    }
    .dark-swal-title {
      color: #fff !important;
      font-size: 16px;
    }
    .dark-swal-text {
      color: #e5e7eb !important;
    }

    .tb-modal {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: rgba(0, 0, 0, 0.7);
      display: none;
      justify-content: center;
      align-items: center;
      z-index: 200;
    }

    .tb-modal-content {
      background: #0b1120;
      padding: 20px;
      border-radius: 12px;
      width: 320px;
      border: 1px solid #1f2937;
    }

    .tb-modal-close {
      margin-top: 12px;
      padding: 10px 12px;
      width: 100%;
      background: #1f2937;
      border: none;
      border-radius: 999px;
      color: #fff;
      cursor: pointer;
    }

    .tb-card--form {
      max-width: 720px;
    }

    /* Form rows */
    .tb-form-row {
      margin-bottom: 14px;
    }

    .tb-label {
      display: block;
      font-size: 12px;
      color: #9ca3af;
      margin-bottom: 4px;
    }

    /* Inputs */
    .tb-input {
      width: 100%;
      padding: 9px 10px;
      border-radius: 8px;
      border: 1px solid #1f2937;
      background: #020617;
      color: #e5e7eb;
      font-size: 13px;
      outline: none;
      transition: border-color 0.15s ease, box-shadow 0.15s ease,
        background 0.15s ease;
    }

    .tb-input::placeholder {
      color: #6b7280;
    }

    .tb-input:focus {
      border-color: #2563eb;
      background: #020617;
      box-shadow: 0 0 0 1px rgba(37, 99, 235, 0.35);
    }
    /* Disable text selection everywhere */
    body {
      user-select: none;
      -webkit-user-select: none;
      -moz-user-select: none;
      -ms-user-select: none;
      caret-color: transparent;
    }

    /* Primary button for forms */
    .tb-btn-primary {
      margin-top: 12px;
      width: 100%;
      padding: 10px 12px;
      border-radius: 999px;
      border: none;
      background: #4f46e5;
      color: #f9fafb;
      font-size: 14px;
      font-weight: 500;
      cursor: pointer;
      box-shadow: 0 14px 26px rgba(15, 23, 42, 0.9);
      transition: background 0.12s ease, transform 0.12s ease,
        box-shadow 0.12s ease;
    }

    .tb-btn-primary:hover {
      background: #6366f1;
      transform: translateY(-1px);
      box-shadow: 0 18px 32px rgba(15, 23, 42, 1);
    }

    .tb-btn-primary:active {
      transform: translateY(1px);
      box-shadow: 0 10px 20px rgba(15, 23, 42, 1);
    }

    /* Compact table for recent prices */
    .tb-table--compact th,
    .tb-table--compact td {
      padding-top: 8px;
      padding-bottom: 8px;
    }

    /* Small icon link in Actions column */
    .tb-icon-link {
      font-size: 14px;
      text-decoration: none;
      color: #60a5fa;
    }

    .tb-icon-link:hover {
      color: #93c5fd;
    }

    /* Shell + theme */
    .tb-body {
      margin: 0;
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
        sans-serif;
      background: #020617;
      color: #e5e7eb;
    }

    .tb-shell {
      display: flex;
      min-height: 100vh;
    }

    /* Sidebar */
    .tb-sidebar {
      width: 220px;
      background: #0b1120;
      color: #e5e7eb;
      padding: 16px 12px;
    }

    .tb-logo {
      font-weight: 600;
      margin-bottom: 24px;
    }

    .tb-nav {
      display: flex;
      flex-direction: column;
      gap: 6px;
    }

    .tb-nav-item {
      padding: 8px 10px;
      border-radius: 6px;
      color: #e5e7eb;
      text-decoration: none;
      font-size: 14px;
    }

    .tb-nav-item--active,
    .tb-nav-item:hover {
      background: #111827;
    }

    /* Main area + top bar */
    .tb-main {
      flex: 1;
      display: flex;
      flex-direction: column;
      background: #020617;
    }

    .tb-topbar {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 14px 24px;
      border-bottom: 1px solid #111827;
      background: #020617;
      position: sticky;
      top: 0;
      z-index: 10;
    }

    .tb-topbar-title h1 {
      margin: 0;
      font-size: 18px;
      font-weight: 600;
      color: #f9fafb;
    }

    /* Profile menu on right */
    .tb-topbar-user {
      display: flex;
      align-items: center;
    }

    .tb-user-menu {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 4px 8px;
      border-radius: 999px;
      background: #020617;
      border: 1px solid #1f2937;
      cursor: pointer;
      position: relative;
    }

    .tb-user-avatar {
      width: 32px;
      height: 32px;
      border-radius: 999px;
      background: #1f2937;
      display: flex;
      align-items: center;
      justify-content: center;
      color: #e5e7eb;
      font-weight: 600;
      font-size: 14px;
    }

    .tb-user-text {
      display: flex;
      flex-direction: column;
      align-items: flex-start;
    }

    .tb-user-name {
      margin: 0;
      font-size: 13px;
      color: #e5e7eb;
    }

    .tb-user-role {
      margin: 0;
      font-size: 11px;
      color: #9ca3af;
    }

    /* Pagination (Dashboard style) */
    .tb-pagination {
      margin-top: 16px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      background: #020617;
      padding: 10px 14px;
      border-radius: 10px;
      border: 1px solid #1f2937;
    }

    .tb-pagination-info {
      font-size: 13px;
      color: #9ca3af;
    }

    .tb-pagination-actions {
      display: flex;
      gap: 8px;
    }

    .tb-page-btn {
      padding: 6px 14px;
      border-radius: 8px;
      font-size: 13px;
      text-decoration: none;
      background: #1f2937;
      color: #e5e7eb;
      border: 1px solid #374151;
      transition: background 0.15s ease;
    }

    .tb-page-btn:hover {
      background: #374151;
    }

    .tb-page-btn--primary {
      background: #4f46e5;
      border-color: #4f46e5;
    }

    .tb-page-btn--primary:hover {
      background: #6366f1;
    }

    .tb-page-btn--disabled {
      opacity: 0.45;
      cursor: not-allowed;
    }

    .tb-user-caret {
      border: none;
      background: transparent;
      color: #9ca3af;
      font-size: 12px;
      padding: 0 4px 0 2px;
      cursor: pointer;
    }

    /* Dropdown */
    .tb-user-dropdown {
      position: absolute;
      right: 0;
      top: 110%;
      min-width: 160px;
      background: #020617;
      border-radius: 10px;
      box-shadow: 0 16px 40px rgba(0, 0, 0, 0.65);
      border: 1px solid #1f2937;
      padding: 6px 0;
      display: none;
      z-index: 20;
    }

    .tb-user-menu.tb-user-menu--open .tb-user-dropdown {
      display: block;
    }

    .tb-user-dropdown a,
    .tb-user-dropdown button {
      width: 100%;
      padding: 8px 14px;
      font-size: 13px;
      color: #e5e7eb;
      text-align: left;
      background: transparent;
      border: none;
      cursor: pointer;
    }
/* ===== DASHBOARD CHART FIX ===== */
.tb-chart-card {
  height: 280px;          /* ✅ FIXED CARD HEIGHT */
  padding: 16px;
}

.tb-chart-wrapper {
  position: relative;
  height: 100%;
  width: 1220px;
}

.tb-chart-wrapper canvas {
  max-height: 100% !important;
}

    .tb-user-dropdown a:hover,
    .tb-user-dropdown button:hover {
      background: #111827;
    }

    /* Content area below top bar */
    .tb-content {
      padding: 20px 28px 28px;
    }

    /* Sections + cards + table (all dark with light text) */
    .tb-section {
      margin-bottom: 28px;
    }

    .tb-section h2 {
      margin: 0 0 12px;
      font-size: 15px;
      color: #e5e7eb;
    }

    .tb-cards {
      display: grid;
      grid-template-columns: repeat(4, minmax(0, 1fr));
      gap: 12px;
    }

    .tb-card {
      background: #0b1120;
      border-radius: 10px;
      padding: 12px 14px;
      box-shadow: 0 1px 3px rgba(15, 23, 42, 0.4);
      color: #f9fafb;
    }

    .tb-card-label {
      margin: 0 0 4px;
      font-size: 12px;
      color: #9ca3af;
    }

    .tb-card-value {
      margin: 0 0 2px;
      font-size: 18px;
      font-weight: 600;
      color: #ffffff;
    }

    .tb-card-meta {
      margin: 0;
      font-size: 11px;
      color: #9ca3af;
    }

    /* Table */
    .tb-table {
      width: 100%;
      border-collapse: collapse;
      background: #0b1120;
      border-radius: 10px;
      overflow: hidden;
      font-size: 13px;
      color: #e5e7eb;
    }

    .tb-table th,
    .tb-table td {
      padding: 10px 12px;
      border-bottom: 1px solid #1f2937;
      text-align: left;
    }

    .tb-table th {
      background: #020617;
      font-weight: 500;
      color: #9ca3af;
    }

    /* Status pills */
    .tb-status {
      display: inline-block;
      padding: 3px 8px;
      border-radius: 999px;
      font-size: 11px;
    }

    .tb-status--confirmed {
      background: #16a34a33;
      color: #4ade80;
    }
    .tb-status--draft {
      background: #4b556333;
      color: #e5e7eb;
    }
    .tb-status--cancelled {
      background: #b91c1c33;
      color: #fca5a5;
    }

    /* Action icons */
    .tb-icon-btn {
      border: none;
      background: transparent;
      cursor: pointer;
      font-size: 13px;
      margin-right: 4px;
      color: #e5e7eb;
    }

    .tb-icon-btn--danger {
      color: #fca5a5;
    }

    /* Chart placeholder */
    .tb-chart-placeholder {
      height: 220px;
      display: flex;
      align-items: center;
      justify-content: center;
      color: #9ca3af;
      font-size: 13px;
      background: #0b1120;
    }

    /* Footer */
    .tb-footer {
      margin-top: 24px;
      font-size: 11px;
      color: #9ca3af;
    }
  </style>
</html>
