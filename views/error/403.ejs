<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
  <meta charset="UTF-8" />
  <title>403 - Forbidden</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <!-- Bootstrap 5 CSS (remove if already in your layout) -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />

  <style>
    :root {
      --bg: #050510;
      --accent: #4f9dff;
      --accent-soft: rgba(79, 157, 255, 0.3);
      --text: #f5f5f5;
      --text-muted: #9ca3af;
      --danger: #f97373;
    }

    body {
      min-height: 100vh;
      background: radial-gradient(circle at top, #111827 0, #020617 45%, #000 100%);
      color: var(--text);
      overflow: hidden;
    }

    .error-card {
      background: rgba(15, 23, 42, 0.85);
      border-radius: 1.5rem;
      box-shadow:
        0 0 40px rgba(15, 23, 42, 0.9),
        0 0 80px rgba(56, 189, 248, 0.15);
      backdrop-filter: blur(16px);
      border: 1px solid rgba(148, 163, 184, 0.35);
      max-width: 520px;
      width: 100%;
    }

    .code {
      font-size: clamp(3.2rem, 7vw, 5rem);
      letter-spacing: 0.15em;
      margin-bottom: 0.75rem;
      text-shadow:
        0 0 15px rgba(248, 250, 252, 0.7),
        0 0 45px rgba(79, 157, 255, 0.8);
    }

    .code span {
      display: inline-block;
      animation: codeGlow 3.5s ease-in-out infinite;
    }

    .code span:nth-child(2) { animation-delay: 0.25s; }
    .code span:nth-child(3) { animation-delay: 0.4s; }

    @keyframes codeGlow {
      0%, 100% {
        text-shadow:
          0 0 10px rgba(148, 163, 184, 0.3),
          0 0 25px rgba(79, 157, 255, 0.5);
        transform: translateY(0);
      }
      20% {
        text-shadow:
          0 0 22px rgba(248, 250, 252, 0.9),
          0 0 55px rgba(79, 157, 255, 1);
        transform: translateY(-2px);
      }
      50% {
        text-shadow:
          0 0 16px rgba(148, 163, 184, 0.6),
          0 0 30px rgba(56, 189, 248, 0.8);
        transform: translateY(1px);
      }
    }

    .subtitle {
      font-size: 0.95rem;
      color: var(--text-muted);
    }

    .hint strong {
      color: var(--danger);
      letter-spacing: 0.05em;
    }

    /* lightning stuff kept from previous version (shortened here for clarity) */
    .lightning-layer,
    .lightning-layer::before,
    .lightning-layer::after {
      position: fixed;
      inset: 0;
      pointer-events: none;
    }
    .lightning-layer {
      background:
        radial-gradient(circle at 10% 0, rgba(59, 130, 246, 0.12) 0, transparent 45%),
        radial-gradient(circle at 90% 0, rgba(96, 165, 250, 0.14) 0, transparent 50%);
      mix-blend-mode: screen;
      opacity: 0.6;
      z-index: 0;
    }
    /* keep bolt, particles, animations from previous file exactly the same */
  </style>
</head>
<body class="d-flex align-items-center justify-content-center">

  <!-- animated lightning background -->
  <div class="lightning-layer"></div>
  <div class="bolt"></div>
  <div class="particle"></div>
  <div class="particle"></div>
  <div class="particle"></div>
  <div class="particle"></div>

  <!-- Bootstrap centered container -->
  <div class="container position-relative z-3">
    <div class="row justify-content-center">
      <div class="col-11 col-sm-9 col-md-7 col-lg-5">

        <div class="error-card mx-auto p-4 p-sm-5 text-center">
          <div class="code mb-2">
            <span>4</span><span>0</span><span>3</span>
          </div>
          <h1 class="h4 mb-2">Access denied</h1>
          <p class="subtitle mb-4">
            The gate is closed for this path. If you think this is a mistake, try signing in with a different account.
          </p>

          <div class="d-flex flex-column flex-sm-row gap-2 justify-content-center">
            <a href="/" class="btn btn-primary px-4">
              Go home
            </a>
            <a href="/login" class="btn btn-outline-light px-4">
              Switch account
            </a>
          </div>

          <p class="hint mt-3 mb-0 text-muted small">
            <strong>FORBIDDEN</strong> · You do not have permission to view this resource.
          </p>
        </div>

      </div>
    </div>
  </div>

  <!-- Bootstrap JS (optional, if not already loaded) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
